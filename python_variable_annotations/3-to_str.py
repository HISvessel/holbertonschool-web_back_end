#!/usr/bin/env python3
"""this function takes an integer and converts it to a string
reinforced with type annotation"""


def to_str(n: float) -> str:
    """takes an float and returns a string"""
    return str(n)
