#!/usr/bin/env python3
"""this function enforces type annotation on function parameters"""


def concat(str1: str, str2: str) -> str:
    """returns the concatenation of two strings
    the function is reinforced with type annotation"""

    return str1 + str2
