function countStudents(path) {
  if (!path) {
    process.stderr("Cannot load the database");
  }

  process.stdout.write("Noice\n");
  process.stdout.write("As\n");
  process.stdout.write("Expected \n");
}

module.exports = countStudents;